{
    "tasks" : [
        {
            "label" : "Build meson release",
            "type" : "shell",
            "command" : "meson build --buildtype=release && ninja -C build",
            "group": {
                "kind" : "build",
                "isDefault": true
            }
        }
    ]
}